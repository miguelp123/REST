spring:
  application:
    name: rest-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_usuarios?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: Support2016*
  jpa:
    hibernate:
      ddl-auto: update
    database-plataform: org.hibernate.dialect.MySQL8Dialect
    properties:
        hibernate:
            jdbc:
                time_zone: UTC
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
